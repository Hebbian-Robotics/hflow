repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Ruff version matches the requirement in pyproject.toml (>=0.1.62 or similar)
    rev: v0.6.2
    hooks:
      # Run the linter.
      - id: ruff
        args: [ --fix ]
      # Run the formatter.
      - id: ruff-format
